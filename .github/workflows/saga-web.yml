name: saga-web

on:
  push:
    branches: [main]
    paths:
      - "apps/saga-web/**"
      - ".github/workflows/saga-web.yml"
      - ".github/workflows/_build-deploy.yml"
      - ".github/actions/gitops-promote/**"
      - ".github/actions/docker-build/**"
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Existing tag to deploy (rollback/promote). Empty = build current."
        required: false

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    uses: ./.github/workflows/_build-deploy.yml
    with:
      app: saga-web
      app_dir: apps/saga-web
      image: ghcr.io/kongebra/saga-web
      image_tag: ${{ github.event.inputs.image_tag || '' }}
    secrets: inherit
